import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.wedmegood.com","randomuser.me"], 
    // Allow images from this domain
  },
  
};

export default nextConfig;
